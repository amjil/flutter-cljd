(ns flutter-cljd.internal.action-listener
  (:require
   ["package:flutter/material.dart" :as m]
   ["dart:async" :as async]))

(defn ^m/ActionListener action-listener*
  [listener action child]
  (m/ActionListener 
   .listener listener 
   .action action 
   .child child))
