(ns flutter-cljd.internal.block-semantics
  (:require
   ["package:flutter/material.dart" :as m]))

(defn ^m/Widget block-semantics*
  [blocking child]
  (m/BlockSemantics .blocking blocking .child child))
